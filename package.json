{
  "name": "hcmnotify-prototype-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && rm -rf ./public && cp -r ../frontend/dist ./public",
    "start": "cd backend && node dist/index.js",
    "postinstall": "cd backend && npm install"
  }
}
